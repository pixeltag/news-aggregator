# Getting Started with New Aggregator

Build the docker image
### `docker build -t react-news-aggregator .`

Run the container
### `docker run -p 3000:80 react-news-aggregator`

Run the test
### `npm test`
